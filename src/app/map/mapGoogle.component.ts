import {AfterViewChecked, AfterViewInit, Component, OnInit, ViewChild} from '@angular/core';
// @ts-ignore
import { } from '@types/googlemaps';

@Component ({
    selector: 'app-map',
    template: `<app-gmap></app-gmap>`
})
export class MapGoogleComponent {
  constructor() {
  }
}